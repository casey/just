#!/usr/bin/env bash

set -euxo pipefail

which rg

! rg \
  --glob !CHANGELOG.md \
  --glob !CHANGELOG.txt \
  --glob !bin/forbid \
  --glob !metadata.yaml \
  --ignore-case \
  'dbg!|fixme|todo|xxx'
